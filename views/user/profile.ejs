<%- include('../partials/head',{user}); %>
    <h1>Data Lengkap Pasien</h1>
    <h6>Nama : <%= patient.Profile.name %></h6>
    <h6>Umur : <%= patient.Profile.age %></h6>
    <h6>Gender : <%= patient.Profile.gender %></h6>
    <h6>History Periksa : </h6> 
    <table border="1">
        <thead>
            <tr>
                <th>No</th>
                <th>Tanggal Periksa</th>
                <th>Dokter yang Memeriksa</th>
                <th>Hasil Diagnosa</th>
                <th>Opsi</th>  
            </tr>
        </thead>
        <tbody>
            <% let i = 1 %> 
            <% patient.userRecord.forEach(cr => { %>
                <tr>
                    <td><%= i %></td>
                    <td><%= cr.checkupDate %></td>
                    <td>dr. <%= cr.Doctor.Profile.name %></td>
                    <td><%= cr.Desease.name %></td>
                    <td>
                        <a href="/user/<%= patient.id %>/checkResult/<%= cr.id %>">Detail</a>
                    </td>
                </tr>
                <% i = i + 1 %> 
            <% }) %>
        </tbody>
    </table>
<%- include('../partials/foot'); %>